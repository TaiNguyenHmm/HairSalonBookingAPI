@page
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quên Mật khẩu - Hair Salon Booking</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@300;400;500;700&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Be Vietnam Pro', sans-serif;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: 'Playfair Display', serif;
        }

        .forgot-password-bg {
            background-color: #f7fafc;
            background-image: linear-gradient(rgba(237, 242, 247, 0.8), rgba(237, 242, 247, 0.8)), url('https://placehold.co/1920x1080/e2e8f0/ffffff?text=Beauty+Salon');
            background-size: cover;
            background-position: center;
        }

        .dark .forgot-password-bg {
            background-color: #1a202c;
            background-image: linear-gradient(rgba(26, 32, 44, 0.9), rgba(26, 32, 44, 0.9)), url('https://placehold.co/1920x1080/2d3748/ffffff?text=Beauty+Salon');
        }
    </style>
</head>
<body class="forgot-password-bg dark:bg-gray-900">

    <div class="min-h-screen flex flex-col items-center justify-center px-4 py-12">

        <div class="text-center mb-8">
            <a href="/" class="text-4xl font-bold text-gray-800 dark:text-white">HairSalon</a>
        </div>

        <div class="max-w-md w-full bg-white dark:bg-gray-800 rounded-2xl shadow-2xl p-8">
            <h2 class="text-3xl font-bold text-center text-gray-900 dark:text-white mb-2">Đặt lại mật khẩu</h2>
            <p class="text-center text-gray-600 dark:text-gray-400 mb-8">Nhập tên đăng nhập và email của bạn. Chúng tôi sẽ gửi một mật khẩu mới vào email đó.</p>

            <form id="forgot-password-form" action="#" method="POST">
                <!-- Thông báo lỗi (hiển thị khi cần) -->
                <div id="error-message" class="hidden bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg relative mb-6" role="alert">
                    <strong class="font-bold">Lỗi!</strong>
                    <span id="error-text" class="block sm:inline">Tên đăng nhập hoặc email không đúng.</span>
                </div>
                <!-- Thông báo thành công (hiển thị khi cần) -->
                <div id="success-message" class="hidden bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded-lg relative mb-6" role="alert">
                    <strong class="font-bold">Thành công!</strong>
                    <span id="success-text" class="block sm:inline">Nếu thông tin hợp lệ, mật khẩu mới đã được gửi đến email của bạn.</span>
                </div>

                <div class="space-y-6">
                    <div>
                        <label for="username" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Tên đăng nhập</label>
                        <div class="mt-1">
                            <input id="username" name="username" type="text" autocomplete="username" required
                                   class="w-full p-3 bg-gray-50 dark:bg-gray-700 border border-gray-300 dark:border-gray-600 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-indigo-500"
                                   placeholder="tên đăng nhập của bạn">
                        </div>
                    </div>

                    <div>
                        <label for="email" class="block text-sm font-medium text-gray-700 dark:text-gray-300">
                            Email</p>
                            <div class="mt-1">
                                <input id="email" name="email" type="email" autocomplete="email" required
                                       class="w-full p-3 bg-gray-50 dark:bg-gray-700 border border-gray-300 dark:border-gray-600 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-indigo-500"
                                       placeholder="you@example.com">
                            </div>
                    </div>

                    <div>
                        <button type="submit"
                                class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-sm text-base font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition duration-300">
                            Gửi yêu cầu
                        </button>
                    </div>
                </div>
            </form>

            <p class="mt-8 text-center text-sm text-gray-600 dark:text-gray-400">
                Nhớ lại mật khẩu?
                <a href="/login" class="font-medium text-indigo-600 hover:text-indigo-500 dark:text-indigo-400 dark:hover:text-indigo-300">
                    Quay lại đăng nhập
                </a>
            </p>
        </div>
    </div>

    <script>
            

        document.addEventListener('DOMContentLoaded', function() {
            const forgotPasswordForm = document.getElementById('forgot-password-form');
            const errorMessageContainer = document.getElementById('error-message');
            const errorText = document.getElementById('error-text');
            const successMessageContainer = document.getElementById('success-message');
            forgotPasswordForm.addEventListener('submit', function(e) {
                e.preventDefault(); // Chặn submit thật
                errorMessageContainer.classList.add('hidden');
                successMessageContainer.classList.add('hidden');
                // Lấy dữ liệu nhập của user
                const username = document.getElementById('username').value;
                const email = document.getElementById('email').value;
                const requestData = { username, email };
                // Gửi AJAX đúng port của WebAPI
                fetch('https://localhost:7144/api/auth/forgot-password', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(requestData)
                })
                        .then(response => {
            if (response.ok) {
                successMessageContainer.classList.remove('hidden');
                forgotPasswordForm.reset();
                // Tự động chuyển hướng sau 3 giây
                setTimeout(() => {
                    window.location.href = '/Authentication/login';
                }, 3000); // 3000 ms = 3 giây
            } else {
                errorText.textContent = 'Tên đăng nhập hoặc email không đúng.';
                errorMessageContainer.classList.remove('hidden');
            }
        })

                .catch(error => {
                    errorText.textContent = 'Không thể kết nối đến máy chủ.';
                    errorMessageContainer.classList.remove('hidden');
                });
            });
        });

    </script>

</body>
</html>
